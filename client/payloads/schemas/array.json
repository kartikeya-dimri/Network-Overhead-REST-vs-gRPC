{
  "description": "Homogeneous array of small objects. Fixed element size, element count varies. Maximizes JSON schema repetition.",
  "structure": "array",
  "parameters": {
    "element_count": "varies (target / ~6)",
    "value_size": "4 chars (fixed per element)",
    "schema_keys": "\"idx\", \"val\" — repeated for every element in JSON"
  },
  "example": [
    { "idx": 0, "val": "aBcD" },
    { "idx": 1, "val": "eFgH" },
    { "idx": 2, "val": "iJkL" }
  ],
  "notes": "Small fixed val size ensures schema repetition ('idx','val' keys + braces per element) dominates over data volume. JSON repeats the full key names for every element; protobuf uses compact field tags."
}
